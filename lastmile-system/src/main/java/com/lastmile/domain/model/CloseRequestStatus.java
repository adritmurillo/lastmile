package com.lastmile.domain.model;

public enum CloseRequestStatus {
    PENDING,    // Solicitud pendiente de revisión
    APPROVED,   // Aprobada por dispatcher
    REJECTED    // Rechazada por dispatcher
}
